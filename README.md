## 1. install deps

```shell
pip install -r requirements.txt
```

## 2. activate venv

```shell
./venv/Scripts/activate (win)
source venv/bin/activate (macOS/Linux)
```

## 3. run app

```shell
python init1.py
```